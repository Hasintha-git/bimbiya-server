package com.bimbiya.server.util.enums;

public enum Status {
    init,draft,active, inactive, deleted, blocked, suspended, pending, reset,expired
    ,approved, rejected, hold,completed, locked, processing, shipped, canceled
}
